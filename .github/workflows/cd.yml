name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: [self-hosted, Linux, X64]
    environment: production
    env:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create .env
        run: |
          {
            echo "MONGO_URI=${{ secrets.MONGO_URI }}"
            echo "DB_NAME=${{ secrets.DB_NAME }}"
            echo "MONGO_INITDB_ROOT_USERNAME=${{ secrets.MONGO_INITDB_ROOT_USERNAME }}"
            echo "MONGO_INITDB_ROOT_PASSWORD=${{ secrets.MONGO_INITDB_ROOT_PASSWORD }}"
            echo "RABBITMQ_HOST=${{ secrets.RABBITMQ_HOST }}"
            echo "RABBITMQ_DEFAULT_USERNAME=${{ secrets.RABBITMQ_DEFAULT_USERNAME }}"
            echo "RABBITMQ_DEFAULT_PASSWORD=${{ secrets.RABBITMQ_DEFAULT_PASSWORD }}"
          } > .env
          chmod 600 .env

      - name: Up (pull from Hub, no build on prod)
        run: |
          docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --pull always --no-build
          docker compose ps